"""Base feature engineering primitives (stub)."""

from __future__ import annotations

import pandas as pd


def build_base_features(frame: pd.DataFrame) -> pd.DataFrame:
    """Return input for now."""
    return frame.copy()
