"""Deterministic feature components (stub)."""

from __future__ import annotations

import pandas as pd


def add_time_features(frame: pd.DataFrame) -> pd.DataFrame:
    """Return input for now."""
    return frame.copy()
