"""Report generation helpers (stub)."""

from __future__ import annotations

from pathlib import Path


def build_report(run_dir: Path) -> Path:
    """Placeholder report builder."""
    return run_dir / "report.md"
